STRCMP() FUNCTION:-STRINGS

#include <stdio.h>
#include<string.h>

int main() {

    char str[50],str1[50];
    scanf("%s",str);
    scanf("%s",str1);
    int n=strcmp(str,str1);
    if(n==0){
        printf("Strings are equal");
    }
    else{
        printf("Strings are not equal");
    }

}
