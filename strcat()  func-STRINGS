STRCAT() FUNCTION -STRINGS

#include <stdio.h>
#include<string.h>

int main() {

    char str[50],str1[50];
    scanf("%s",str);
    scanf("%s",str1);
    strcat(str," ");
    strcat(str,str1);
    printf("%s",str);
    

    return 0;
}
