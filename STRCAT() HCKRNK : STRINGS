STRCAT() HACKERRANK PROBLEM:

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    char str1[50],str2[50];
    scanf("%[^\n]s",str1);
    scanf("%s[^\n]s",str2);
    strcat(str1," ");
    strcat(str1,str2);
    printf("%s",str1);
    return 0;
}
